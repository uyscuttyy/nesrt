[package]
name = "mock_lender"
version = "0.1.0"
description = "Mock lending pool for TSLAx - simulates Kamino Lend CPI interface for Devnet"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_lender"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"