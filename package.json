{
  "name": "tslax-vault",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup:devnet": "ts-node setup/setup_devnet.ts",
    "test": "anchor test --provider.cluster devnet"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
